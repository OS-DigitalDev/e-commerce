import ProductGrid from "../components/ProductGrid";

function Product() {
  return (
    /* Use 'main' for better SEO and accessibility */
    /* Use a class to manage luxury spacing (padding-top) */
    <main className="product-page-wrapper">
        
        
        <ProductGrid />
    </main>
  );
}

export default Product;
